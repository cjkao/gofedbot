when bot join federation as bot role
it heard 2 messages, bot should ignore message with same origin

# how to purge?
all federate message will be sync across server,
only purge 1 site does not remove all messages, federate event will send all src server's message to dest when dest is not exist
